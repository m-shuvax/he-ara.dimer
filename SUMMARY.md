1. קהל יעד B2C
המודל של הdataBase התאים יותר לזה
 2. gpt & claude & copilot
אחרי שגמרתי את הbackend ביקשתי לעבור על הקוד ושאלתי מה אפשר לייעל
ובפרונט ביקשתי עזרה לעיצוב.
3. חיבור  בין ה־Frontend ל־Backend, כולל עבודה מול בסיס נתונים מקומי (SQLite)
האתגר נפתר אחרי כמה בדיקות ידניות ועזרה בכלי AI.
4. הייתי מוסיף מערכת ניהול לידים ומערכת סטיטיסקה וביקוש לפי מוצר
5.
הוראות הרצה:

יש להריץ מתוך תיקיית ה־backend:

npm install
npm start

יש להריץ מתוך תיקיית ה־frontend:

npm install
npm run dev